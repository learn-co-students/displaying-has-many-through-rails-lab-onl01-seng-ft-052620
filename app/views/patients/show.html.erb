<%= @patient.name %>
<ul>
<% @patient.appointments.each do |ap| %>
    <li>
    <%= ap.appointment_datetime.strftime("%B %e, %Y at %H:%M") %>
    <%= link_to ap.doctor.name, doctor_path(ap.doctor) %>
    </li>
<% end %>
</ul>
