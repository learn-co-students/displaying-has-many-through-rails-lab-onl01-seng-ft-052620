<h1><%=@doctor.name%></h1>
<h2><%=@doctor.department%></h2>
<ul>
<%=@doctor.appointments.each do |appt|%>
    <li>
        Date: <%=appt.appointment_datetime.strftime('%B %d, %Y at %H:%M')%>
        Patient: <%= link_to appt.patient.name, patient_path(appt.patient) %>
    </li>
<%end %>

</ul>

