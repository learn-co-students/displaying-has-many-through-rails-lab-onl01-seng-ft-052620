<h1>Patients</h1>

  <ul>
    <% @patients.each do |patient| %>
      <li>Name: <%= link_to patient.name, patient_path(patient.id) %></li>
      <li>Number of Appointments: <%= patient.appointments.length %></li>
    <% end %>
  </ul>