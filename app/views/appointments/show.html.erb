<h1>Appointments</h1>

<li> <%= link_to @appointment.doctor.name, doctor_path(@appointment.doctor)   %></li>
<li> <%= link_to @appointment.patient.name, patient_path(@appointment.patient)   %></li>
    <p><%= @appointment.appointment_datetime.strftime("%B %d, %Y %H:%M") %></p>