<h2><%= @doctor.name %></h2>
<h2><%= @doctor.department %></h2>

<uL>
  <%=@doctor.appointments.each do |app| %>
    <li>
       Date: <%=app.appointment_datetime.strftime('%B %d, %Y at %H:%M')%>
       Patient: <%= link_to app.patient.name, patient_path(app.patient) %>
    </li>
  <% end %>
  </ul>