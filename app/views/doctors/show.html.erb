<h2>Dr. <%= @doctor.name %></h2>
<ul>
    <% @doctor.appointments.each do |appt| %>
    <li>
        <label>Date & Time: </label><%= appt.appointment_datetime.strftime("%B %d, %Y at %H:%M") %>
        <label>Patient: </label><%= link_to appt.patient.name, patient_path(appt.patient) %>
    </li>
   <% end %>
</ul>